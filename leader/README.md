The pipeline leader is a script (leader.py) that uses the pydatatask library and a bunch of resource and task definitions to determine what to run and when.
It will be built automatically by ../build.sh.
